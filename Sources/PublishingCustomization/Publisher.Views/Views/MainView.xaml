<UserControl x:Class="Publisher.Views.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Publisher.Views.Views"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Menu FontSize="20">
            <MenuItem Header="Messengers">
                <MenuItem Header="Telegram"/>
                <MenuItem Header="Twitter"/>
                <MenuItem Header="Tiktok"/>
                <MenuItem Header="Linkedin"/>
                <Separator></Separator>
                <MenuItem Header="Exit"></MenuItem>
            </MenuItem>
        </Menu>
        <Label Grid.Row="1" Content="Choose a messenger from the menu" FontSize="22" Background="AliceBlue"/>
    </Grid>
</UserControl>
